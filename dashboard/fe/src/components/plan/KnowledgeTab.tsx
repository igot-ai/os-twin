'use client';

/**
 * @deprecated This component is deprecated. Use one of these instead:
 * - PlanKnowledgeTab: For use within PlanContext (bridges plan-specific features)
 * - KnowledgeTabCore: For standalone use without PlanContext
 * 
 * This file re-exports PlanKnowledgeTab for backward compatibility.
 */

// Re-export PlanKnowledgeTab for backward compatibility
export { default } from './PlanKnowledgeTab';
