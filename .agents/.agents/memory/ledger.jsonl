{"id": "mem-cod-1775110212913113000-47333", "ts": "2026-04-02T06:10:12Z", "kind": "code", "room_id": "room-003", "author_role": "engineer", "ref": "EPIC-003", "tags": ["deadlock", "recovery", "lifecycle", "role"], "summary": "roles/manager/Start-ManagerLoop.ps1 \u2014 Fixed Risk 6 in deadlock recovery", "detail": "Deadlock recovery now resolves role from lifecycle state definition ($dlStateDef.role) instead of config.json. This ensures multi-role lifecycles (e.g. reporting.role=reporter) restart with the correct runner. Restart role is also resolved from the target state's definition."}
{"id": "mem-dec-1775110213112053000-47341", "ts": "2026-04-02T06:10:13Z", "kind": "decision", "room_id": "room-003", "author_role": "engineer", "ref": "EPIC-003", "tags": ["architecture", "deadlock", "roles"], "summary": "Resolved deadlock recovery role from lifecycle state", "detail": "Chose to resolve roles from lifecycle states to support complex pipelines where the active role might differ from the room's primary assigned_role. Verified via Risk 6 tests in Deadlock.Tests.ps1."}
{"id": "mem-cod-1775110223234198000-47449", "ts": "2026-04-02T06:10:23Z", "kind": "code", "room_id": "room-002", "author_role": "engineer", "ref": "EPIC-002", "tags": ["deadlock", "manager", "risk3", "risk4"], "summary": "/Users/paulaan/PycharmProjects/agent-os/.agents/roles/manager/Start-ManagerLoop.ps1 \u2014 Deadlock recovery fix", "detail": "Risk 3+4 fix: DO NOT increment retries during deadlock recovery. Retries should only be incremented by lifecycle signal actions (e.g. increment_retries on QA fail). Incrementing retries during deadlock recovery corrupts the done-count gate (Risk 3) and compounds into QA cascade deadlocks (Risk 4). Exhaustion is handled by the deadlock_recoveries cap (line 1113), not by lifecycle retries."}
{"id": "mem-cod-1775110238504621000-47611", "ts": "2026-04-02T06:10:38Z", "kind": "code", "room_id": "room-001", "author_role": "engineer", "ref": "EPIC-001", "tags": ["deadlock", "pids", "spawn"], "summary": "roles/manager/Start-ManagerLoop.ps1 \u2014 Deadlock recovery fix", "detail": "\n                    # Risk 2 fix: Clean stale PIDs before transition (prevents double retry increment)\n                    Stop-RoomProcesses $rd\n\n                    # Risk 3+4 fix: DO NOT increment retries here.\n                    # Retries should only be incremented by lifecycle signal actions (e.g. increment_retries on QA fail).\n                    # Incrementing retries during deadlock recovery corrupts the done-count gate (Risk 3)\n                    # and compounds into QA cascade deadlocks (Risk 4).\n                    # Exhaustion is handled by the deadlock_recoveries cap (line 1113), not by lifecycle retries.\n\n                    # Risk 6 fix: Resolve restart state's role from lifecycle for correct runner\n                    $restartStateDef = if ($dlLifecycle.states.$restartState) { $dlLifecycle.states.$restartState } else { $null }\n                    $dlRestartRole = if ($restartStateDef -and $restartStateDef.role) { ($restartStateDef.role -replace ':.*$', '') } else { $dlRole }\n\n                    Write-Log \"WARN\" \"[$lt] Deadlock recovery ($($dlCount+1)/3): restarting $dlRestartRole via $restartState.\"\n                    & $postMessage -RoomDir $rd -From \"manager\" -To $dlRestartRole -Type \"fix\" -Ref $lt -Body \"Deadlock recovery: restarting $dlRestartRole.\"\n                    Write-RoomStatus $rd $restartState\n\n                    # Risk 2 fix: Spawn worker immediately (don't rely on next iteration's respawn branch)\n                    $dlResolveRole = Join-Path $agentsDir \"roles\" \"_base\" \"Resolve-Role.ps1\"\n                    if (Test-Path $dlResolveRole) {\n                        $dlResolved = & $dlResolveRole -RoleName ($restartStateDef.role) -AgentsDir $agentsDir -WarRoomsDir $WarRoomsDir\n                        Start-WorkerJob -RoomDir $rd -Role $dlRestartRole -Script $dlResolved.Runner -TaskRef $lt -SkipLockCheck\n                    }"}
{"id": "mem-dec-1775110238572943000-47621", "ts": "2026-04-02T06:10:38Z", "kind": "decision", "room_id": "room-001", "author_role": "engineer", "ref": "EPIC-001", "tags": ["deadlock", "architecture"], "summary": "Spawn worker immediately with -SkipLockCheck during deadlock recovery", "detail": "Why: prevents stale PIDs from blocking respawn on next iteration and avoids double retry increment"}
