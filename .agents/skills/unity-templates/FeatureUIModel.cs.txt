using Sun.Core.UI;
using UniRx;

namespace Game.{Feature}.UI.Models
{
    /// <summary>
    /// UI ViewModel for {Feature} screen.
    /// Extends Sun.Core.UI.ViewModel for UIManager integration.
    /// </summary>
    public class {Feature}UIModel : ViewModel
    {
        // Reactive UI state
        public BoolReactiveProperty IsInteractable { get; set; } = new(false);
        public ReactiveProperty<string> Title { get; set; } = new("");

        // Action callbacks (set by Presenter)
        public System.Action OnAction { get; set; }
        public System.Action OnClose { get; set; }

        // Sub-models for child views
        // public ChildViewModel ChildView { get; set; } = new();
    }
}
