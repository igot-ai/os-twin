using Cysharp.Threading.Tasks;
using UniRx;
using UnityEngine;
using VContainer;

namespace Game.{Feature}.Views
{
    /// <summary>
    /// MonoBehaviour view for {Feature}. Subscribes to model state and renders visuals.
    /// </summary>
    public class {Feature}View : MonoBehaviour
    {
        // Serialized references
        // [SerializeField] private SpriteRenderer _renderer;

        private CompositeDisposable _disposables = new();

        /// <summary>
        /// Initialize the view with dependencies.
        /// Called after DI injection or manually from Controller.
        /// </summary>
        public async UniTask Initialize(/* dependencies */)
        {
            _disposables.Clear();
            // TODO: Setup initial visual state
            await UniTask.CompletedTask;
        }

        /// <summary>
        /// Bind to a model's reactive properties.
        /// </summary>
        public void Bind(/* model */)
        {
            // Example:
            // model.State.Subscribe(OnStateChanged).AddTo(_disposables);
        }

        // private void OnStateChanged(E{Feature}State state)
        // {
        //     // Update visuals based on state
        // }

        private void OnDestroy()
        {
            _disposables.Dispose();
        }
    }
}
