using UnityEngine;

namespace Game.{Feature}.Config
{
    [CreateAssetMenu(fileName = "{Feature}Config", menuName = "Game/{Feature}Config")]
    public class {Feature}Config : ScriptableObject
    {
        [Header("Gameplay Settings")]
        [Tooltip("Duration of a single round in seconds")]
        public float RoundDuration = 60f;

        [Tooltip("Maximum number of attempts")]
        public int MaxAttempts = 3;

        [Header("Visual Settings")]
        [Tooltip("Animation speed multiplier")]
        public float AnimationSpeed = 1f;

        // TODO: Add feature-specific configuration fields
    }
}
