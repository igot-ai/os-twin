using UniRx;
using UnityEngine;

namespace Game.{Feature}.Data
{
    /// <summary>
    /// Data model for a single {Feature} entity.
    /// Contains reactive state for view binding.
    /// </summary>
    public class {Feature}Data
    {
        public int Id { get; }
        public ReactiveProperty<E{Feature}EntityState> State { get; } = new(E{Feature}EntityState.Default);

        // TODO: Add entity-specific fields

        public {Feature}Data(int id)
        {
            Id = id;
        }
    }

    /// <summary>
    /// State for individual {Feature} entities.
    /// </summary>
    public enum E{Feature}EntityState
    {
        Default,
        Active,
        Completed,
        Disabled
    }
}
