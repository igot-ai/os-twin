using UniRx;

namespace Game.{Feature}.Models
{
    /// <summary>
    /// ViewModel for {Feature}. Holds reactive state consumed by views and UI.
    /// Registered in DI scope as Lifetime.Scoped.
    /// </summary>
    public class {Feature}Model
    {
        public ReactiveProperty<float> Progress { get; } = new(0f);
        public ReactiveProperty<int> Score { get; } = new(0);

        // TODO: Add feature-specific reactive properties
    }
}
